# AI PM Dev 产品方向、可视化体验与重构判断

> 这份文档记录当前阶段对 `ai-pm-dev` 后续产品化方向的判断：它不应该停留在“AI 帮我写代码”的工具层，而应该演进成一个帮助普通人从 0 到 1 做出可落地产品的 AI 产品交付系统。

## 1. 核心判断

当前最重要的方向判断是：

**`ai-pm-dev` 不应该只是一个 AI Coding 辅助工具，而应该成为“想法到可上线产品”的交付操作系统。**

用户真正需要的不是一段代码，而是：

- 我这个想法值不值得做；
- 应该先做哪一版；
- 哪些功能应该砍掉；
- 应该长什么样；
- 需要什么页面、数据、接口和权限；
- AI 可以自动做哪些，哪些必须人工确认；
- 生成出来的东西能不能跑；
- 距离上线还差什么；
- 用户反馈回来后怎么继续迭代。

所以产品主线应该从：

```text
prompt → code
```

升级为：

```text
idea → clarified PRD → MVP scope → design → architecture → code → test → deploy → feedback → next iteration
```

一句话：**前者是功能，后者才是产品。**

## 2. 目标用户

这个产品的目标不是只服务程序员，而是服务“有产品想法但缺完整交付能力”的人。

主要用户可以分为四类：

1. **非技术创业者 / 业务人员**
   - 有业务想法；
   - 不会写代码；
   - 不知道如何把想法拆成产品；
   - 需要 AI 带着走完需求、原型、开发、部署。

2. **产品经理 / 运营 / 增长人员**
   - 能描述用户痛点；
   - 缺少工程落地能力；
   - 需要快速把方案变成 Demo 或内部工具。

3. **会一点技术的独立开发者**
   - 能看懂代码；
   - 但需要 AI 提升速度；
   - 更关心流程、质量、部署和可维护性。

4. **研发 / 技术负责人**
   - 不缺编码能力；
   - 需要把需求澄清、方案评审、测试、文档和交付闭环自动化。

这意味着默认体验必须足够简单，但底层能力必须足够工程化。

## 3. 产品应该解决的核心问题

### 3.1 需求不是一句 prompt，而是一个澄清过程

用户通常不会一开始就说清楚需求。

比如用户说：

```text
我想做一个健身 App。
```

系统不应该立刻生成代码，而应该先追问：

- 目标用户是谁？
- 用户现在遇到什么问题？
- 第一版只验证什么？
- 是记录训练、生成计划，还是社群打卡？
- 需要登录吗？
- 需要 AI 推荐吗？
- 数据从哪里来？
- 成功指标是什么？
- 哪些功能第一版明确不做？

最终产物应该是一份可执行的轻量 PRD，而不是一段泛泛而谈的回答。

### 3.2 需要自动判断任务类型

系统应该能判断一个需求适合哪种处理方式：

- 一句 prompt 就能解决；
- 需要固定工作流；
- 需要多步骤 Agent；
- 需要多 Agent 协同；
- 需要人工审核；
- 需要专业研发介入；
- 当前 AI 能力不稳定，不适合自动化。

这可以成为系统里的“任务路由器”。

判断示例：

| 用户需求 | 系统判断 | 推荐处理 |
| --- | --- | --- |
| 生成一个落地页 | 简单生成任务 | 直接生成页面 |
| 做一个表单收集系统 | 标准 CRUD | 套模板 + 生成数据库 |
| 做 AI 简历优化工具 | AI 应用 | 需要 Prompt、模型调用、输出校验 |
| 做支付订阅 SaaS | 生产级系统 | 需要鉴权、支付、风控、部署检查 |
| 自动操作用户账号 | 高风险 Agent | 必须人工确认和权限控制 |

### 3.3 AI 应用必须内置工程兜底

Agent 和传统接口不同。

传统接口更像：

```text
input A → output B
```

Agent 更像：

```text
input A → output B / C / D，甚至明天因为模型更新变成 E
```

所以系统必须默认内置：

- 输出格式校验；
- JSON Schema 校验；
- 重试；
- 超时；
- 降级；
- 兜底回答；
- 人工确认节点；
- 调用日志；
- 任务回放；
- Prompt 版本管理；
- 模型版本记录；
- 成本统计；
- 风险提示；
- 部署前检查。

这部分是从“Demo 玩具”到“可落地产品”的分水岭。

## 4. 可视化体验判断

### 4.1 可以学习 Marvis，但不要只复制表层动效

Marvis 的价值不只是动画好看，而是它让复杂任务有了“正在发生”的感觉。

应该学习的是：

- 轻盈的动效；
- 明确的阶段推进；
- AI 当前状态可感知；
- 每一步都有产物；
- 复杂流程被拆成可理解的小节点；
- 用户始终知道下一步该做什么；
- 不让用户一上来面对复杂工作流图。

迁移到 `ai-pm-dev` 上，应该做成“产品生命线”：

```text
想法输入
→ 需求澄清中
→ PRD 已生成
→ MVP 范围已裁剪
→ 页面设计中
→ 技术方案生成中
→ 代码生成中
→ 测试中
→ 沙箱部署中
→ 可访问产品已完成
→ 收集反馈
→ 下一轮迭代
```

用户看到的不是冷冰冰的日志，而是一个产品被 AI 一步步孵化出来。

### 4.2 默认界面不要做成 n8n 那种节点图

n8n、Dify 这类工具很强，但默认心智偏专业。

普通用户一看到：

- 节点；
- 连线；
- Webhook；
- Credential；
- 变量；
- API；
- 分支；
- 数据映射；
- 工具参数；

很容易直接放弃。

所以默认界面不应该是“工作流编辑器”，而应该是：

```text
AI 产品教练 + 产品生命线 + 当前产物 + 风险提示 + 下一步按钮
```

节点图应该放在高级模式里，而不是默认入口。

### 4.3 推荐的三层体验结构

#### 第一层：普通用户视图

面向非技术用户。

展示方式：

- 阶段卡片；
- 当前 AI 正在做什么；
- 当前产物；
- 下一步建议；
- 风险提示；
- 一键继续 / 修改 / 重试；
- 可运行 Demo 预览。

示例：

```text
当前阶段：MVP 范围裁剪

AI 判断：
第一版建议只做 3 个功能：
1. 用户注册登录
2. 创建训练目标
3. AI 生成一周训练计划

暂不建议做：
- 社区
- 支付
- 教练入驻
- Apple Watch 同步

原因：
这些会显著增加开发和验证成本，且不是验证核心需求所必须。
```

#### 第二层：专业流程视图

面向产品经理、研发、独立开发者。

可以展示：

- 任务拆解；
- Agent 分工；
- 人工审核节点；
- 数据流；
- 工具调用；
- 失败重试；
- 测试结果；
- 部署链路。

这一层可以使用 React Flow 这类 node-based UI 框架实现。

#### 第三层：执行引擎视图

面向系统内部和高级调试。

展示：

- 每次 LLM 调用；
- Prompt 版本；
- 模型；
- Token / 成本；
- 工具调用参数；
- 输出校验结果；
- 错误堆栈；
- 重试记录；
- 人工干预记录；
- 回放入口。

这一层决定系统能不能生产化。

## 5. n8n / Dify / LangGraph / React Flow 的取舍

### 5.1 n8n

n8n 适合做外部工具自动化和集成。

适合场景：

- 连 Google Sheet；
- 发邮件；
- 调 Webhook；
- 同步 CRM；
- 自动通知 Slack / 飞书；
- 做运营自动化；
- 做数据流转。

不适合成为默认产品界面。

原因：

- 节点图对普通人门槛高；
- 工作流心智偏自动化，不是产品交付心智；
- 用户会被技术细节淹没。

建议定位：

```text
n8n = 后台集成能力 / 高级自动化插件 / 外部流程执行器
```

### 5.2 Dify

Dify 适合快速搭建 AI 应用、Agent、RAG、Chatbot 和工作流。

适合场景：

- 快速验证一个 AI 应用；
- 管理 Prompt；
- 配置知识库；
- 做聊天机器人；
- 通过 API 接入 AI 能力。

不建议完全依赖 Dify 作为主系统底座。

原因：

- 它解决的是“AI 应用构建”，不是完整产品交付；
- 你的核心体验应该是“从想法到产品”，而不只是“配置一个 AI app”；
- 过早绑定会限制你的交互形态和产品边界。

建议定位：

```text
Dify = 可参考 / 可接入 / 可作为部分 AI 能力原型，但不是主产品形态
```

### 5.3 LangGraph

LangGraph 适合做复杂 Agent 编排、长任务、状态持久化、人类审核和断点恢复。

适合场景：

- 多步骤 Agent；
- 长时间任务；
- 执行中断后恢复；
- human-in-the-loop；
- 状态机；
- 任务回放；
- 多 Agent 协作。

建议定位：

```text
LangGraph = 后期 Agent 编排与生产级执行引擎的重要候选
```

早期不一定马上引入，但设计上要预留“状态机 + 断点恢复 + 人工审核”的抽象。

### 5.4 React Flow

React Flow 适合做节点式 UI。

适合场景：

- 高级流程视图；
- Agent 编排图；
- 任务依赖图；
- 数据流图；
- 产品交付流程图。

建议定位：

```text
React Flow = 专业模式/高级模式的可视化画布
```

不建议把它作为普通用户默认首页。

## 6. 推荐产品架构

推荐架构：

```text
前台体验层
  - 产品生命线
  - 阶段卡片
  - AI 教练对话
  - Demo 预览
  - 风险提示
  - 产物面板

专业可视化层
  - React Flow 节点图
  - Agent 分工
  - 人工审核节点
  - 工具调用链路
  - 测试/部署状态

执行编排层
  - 轻量状态机
  - 任务队列
  - Agent 调用
  - 工具调用
  - 重试/降级/暂停/恢复

项目资产层
  - PRD
  - MVP scope
  - UI spec
  - architecture
  - API contract
  - acceptance tests
  - decision log
  - troubleshooting

工程落地层
  - 代码生成
  - 测试
  - 沙箱部署
  - 正式部署
  - 监控
  - 反馈回流
```

早期可以简化为：

```text
自研前端体验 + 轻量状态机 + 当前 CLI/workflow 能力
```

后期再接：

```text
React Flow + LangGraph + n8n/Dify 集成 + 生产级观测
```

## 7. 推荐产品界面

一个比较理想的界面结构：

```text
┌──────────────────────────────────────────────┐
│ 项目：AI 健身计划助手                         │
├──────────────┬────────────────┬──────────────┤
│ 产品生命线    │ 当前工作区       │ 产物 / 风险   │
│              │                │              │
│ ✓ 想法澄清    │ AI 正在裁剪 MVP  │ PRD          │
│ ✓ 用户定位    │                │ scope        │
│ ● MVP 范围    │ 建议先做 3 个功能 │ UI spec      │
│ ○ 页面设计    │                │ risks        │
│ ○ 技术方案    │ [接受] [修改]    │ next steps   │
│ ○ 代码生成    │                │              │
│ ○ 测试部署    │                │              │
└──────────────┴────────────────┴──────────────┘
```

底部可以有一个执行条：

```text
当前任务：生成 MVP scope
状态：等待用户确认
成本：$0.03
耗时：48s
下一步：生成页面结构
```

## 8. 阶段路线图

### Phase 1：从 CLI workflow 到产品化工作台

状态：第一版已落地为 `apps/web`。它以现有 CLI 作为 workflow kernel，提供读写轻交互的 Web 工作台。

目标：先把现有 `ai-pm-dev` 的强项视觉化。

重点功能：

- 项目创建；
- 想法输入；
- 需求澄清；
- PRD 生成；
- MVP 范围裁剪；
- 产物卡片；
- 阶段进度；
- 下一步建议；
- 当前文档预览。

这一阶段不要急着做复杂节点编辑器。

当前实现边界：

- Web 工作台读取 `.ai-pm-dev/` 和 `docs/`；
- 展示阶段生命线、产物卡片、PRD gate 和下一步建议；
- 通过现有 CLI 触发 quick PRD、`prd check`、checkpoint 和 decision；
- 不引入 React Flow、LangGraph、Dify/n8n 或自研 Agent runtime。

### Phase 2：加入 Demo 生成与交付闭环

目标：让用户真的看到可运行产品。

重点功能：

- 页面生成；
- 技术方案生成；
- 简单代码生成；
- 本地预览；
- 测试清单；
- 沙箱部署；
- 发布前 checklist；
- 用户反馈入口。

### Phase 3：加入专业模式和可视化编排

目标：服务更专业的用户。

重点功能：

- React Flow 流程图；
- Agent 分工图；
- 人工审核节点；
- 工具调用轨迹；
- 任务重试；
- 执行日志；
- 成本统计；
- Prompt 版本管理。

### Phase 4：生产级 AI 产品交付系统

目标：从“生成 Demo”变成“持续运营产品”。

重点功能：

- 长任务恢复；
- 断点续跑；
- 多 Agent 协作；
- 任务回放；
- 线上监控；
- 用户反馈自动归因；
- 迭代建议；
- 版本管理；
- 回滚；
- 多项目管理；
- 团队协作。

## 9. 当前项目是否需要改目录结构

结论：

**现在不建议立刻大规模重构目录结构，但后续如果要从 CLI 工具升级为 Web 产品，目录结构一定要改。**

原因：

当前项目定位仍然是：

```text
Local Idea-to-Build workflow CLI
```

目前目录结构也符合这个定位：

```text
bin/              CLI 入口
scripts/          初始化和任务脚本
skills/           AI 协作技能
templates/        文档模板
operating-layer/  安装到目标项目的操作层
memory/           工具自身的记忆/规则候选
tests/            CLI 和规则测试
examples/         示例项目
```

这个结构对 CLI 是合理的。

但如果后面要做 Web 工作台、可视化流程、后台任务和 Agent 执行，就会需要一个更清晰的 monorepo 结构。

### 9.1 短期不重构，只补文档与边界

短期建议：

- 不移动现有 `bin/`、`scripts/`、`skills/`；
- 不破坏现有 CLI 测试；
- 新增产品战略文档；
- 后续新增 Web 原型时，先放在独立目录，避免污染 CLI。

### 9.2 中期建议改成 monorepo

当开始做 Web UI 时，建议演进为：

```text
ai-pm-dev/
  apps/
    web/                 # 产品工作台：生命线、卡片、预览、专业流程图
    docs-site/           # 可选：官网/文档站

  packages/
    cli/                 # 当前 CLI 能力迁移到这里
    workflow-core/        # PRD、scope、handoff、doctor、gate 等核心逻辑
    agent-runtime/        # 状态机、任务执行、重试、人工审核
    ui-kit/               # 产品卡片、阶段条、动效组件
    visual-flow/          # React Flow 节点和流程视图
    templates/            # 当前 templates 迁移
    skills/               # 当前 skills 迁移或生成包

  examples/
    quick-date/
    ai-fitness/

  docs-product/
    vision.md
    architecture.md
    roadmap.md
    decisions/

  tests/
    e2e/
    fixtures/
```

注意：不要使用根目录 `docs/` 存放本仓库文档，因为当前 `.gitignore` 忽略了 `/docs/`。这是为了防止 CLI 在本仓库运行时生成的目标项目文档污染工具仓库。

### 9.3 更推荐的迁移节奏

不要一次性大迁移。

推荐节奏：

1. 先抽 `workflow-core`
   - 把 PRD、scope、handoff、doctor、template 渲染等逻辑从 CLI 中抽出来；
   - CLI 和 Web 都调用同一套核心能力。

2. 再建 `apps/web`
   - 先做只读工作台；
   - 能展示阶段、文档、下一步；
   - 不急着做全套执行引擎。

3. 再加 `agent-runtime`
   - 状态机；
   - job queue；
   - retry；
   - human-in-the-loop；
   - execution log。

4. 最后做 `visual-flow`
   - 高级流程图；
   - Agent 编排图；
   - 工具调用链路。

## 10. 最小可行产品建议

如果现在要做第一个产品化版本，建议只做这条主链：

```text
输入想法
→ AI 追问
→ 生成 PRD
→ 生成 MVP scope
→ 生成页面结构
→ 生成技术方案
→ 展示交付 checklist
```

这一版不一定要自动写完整代码。

更重要的是先验证：

- 用户是否愿意被 AI 一步步引导；
- 需求澄清是否真的降低门槛；
- MVP 裁剪是否有价值；
- 可视化生命线是否让用户更有掌控感；
- 用户是否愿意继续进入代码生成和部署。

## 11. 产品原则

后续设计和重构都应该遵守这些原则：

1. **默认简单，高级可展开**
   - 普通用户看到产品生命线；
   - 专业用户可以打开流程图和日志。

2. **每一步必须有产物**
   - 不要只有聊天；
   - 每个阶段都应该生成可复用文档或代码资产。

3. **AI 不直接替用户做高风险决策**
   - 支付、权限、数据删除、上线发布等动作必须有人工确认。

4. **不要让用户一开始面对工程复杂度**
   - 复杂度应该被系统托住，而不是直接甩给用户。

5. **生产能力前置**
   - 校验、重试、降级、日志、回放、成本统计不是后期锦上添花，而是 AI 产品可落地的基础。

6. **工作流是手段，不是产品本身**
   - 用户要的是产品被做出来；
   - 不是学会配置一个复杂工作流。

## 12. 最终形态

最终的 `ai-pm-dev` 可以成为：

> 一个 AI 产品交付操作系统。用户输入一个想法，系统自动拆解成产品任务、设计任务、研发任务、测试任务、部署任务和运营任务，并通过 AI Agent + 人工审核节点协同完成，最终交付一个可访问、可测试、可迭代的真实产品。

最终用户感知到的是：

```text
我不是在和一个聊天机器人对话。
我是在和一个 AI 产品团队一起把东西做出来。
```

这就是这个项目后续最值得押注的方向。
